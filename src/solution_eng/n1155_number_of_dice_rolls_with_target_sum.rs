use super::Solution;

impl Solution {
    pub fn num_rolls_to_target(n: i32, k: i32, target: i32) -> i32 {
        let target = target as usize;
        let (mut prev, mut curr) = (vec![0; target], vec![0; target]);
        1
    }
}
